name: Send reminder emails

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Call reminder email endpoint
        run: |
          curl -sf -X POST "${{ secrets.REMINDER_EMAIL_URL }}/api/cron/send-reminder-emails" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
